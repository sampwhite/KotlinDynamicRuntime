/-
A component's own overlay of `sampleContent` (issue #456), read from `<fileId>_overlay.md`.

It names one key. That is the point of an overlay: it does not restate the file, so the keys it says nothing
about keep whatever the base says -- including keys the base has not grown yet.
-/

# @welcome

# +support Contact the sample desk if you need help.
