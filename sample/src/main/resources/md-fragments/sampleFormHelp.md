/- Layout copy help (issue #605): a BACKEND fragment file, never served, pulled by a g-layout's
   description with %{@t("questionnaire.topicHelp")}. Demonstrates a layout pulling shared copy from a
   fragment file, resolved server-side at delivery. -/

# @questionnaire

# +topicHelp Pick the subject these answers are about — pulled from a shared fragment file.
